import React from 'react'

import LinkContainer from '../../../Components/Links/sucessLink'

export default {
  title: 'Pages/Link',
  component: LinkContainer
}

const Template = () => {
  return <LinkContainer />
}

export const Default = Template.bind({})
